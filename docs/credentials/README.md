# Credentials Folder

This folder is for local-only credential inventories.

Allowed local file:

- `credentials.local.md`

Rules:

- Do not commit real credentials.
- Do not copy secrets into normal docs.
- Keep only credentials useful for SuperSites.
- Rotate credentials if they were exposed in an unsafe place.

