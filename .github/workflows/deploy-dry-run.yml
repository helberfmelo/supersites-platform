name: Deploy Dry Run

on:
  push:
    branches:
      - main
    paths:
      - apps/**
      - packages/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - infra/deployment/**
      - scripts/**
      - .github/workflows/deploy-dry-run.yml
      - .github/workflows/deploy-supersite-hostgator.yml
      - .github/workflows/deploy-control-plane-hostgator.yml
      - .github/workflows/deploy-netprobe-hostgator.yml
      - .github/workflows/deploy-static-app-hostgator.yml
  workflow_dispatch:
    inputs:
      target_environment:
        description: Target environment name for the dry-run plan.
        required: true
        default: staging-hostgator
        type: choice
        options:
          - staging-hostgator
          - production-hostgator

permissions:
  contents: read

jobs:
  deploy_plan:
    name: Generate deploy dry-run plan
    runs-on: ubuntu-latest
    environment: ${{ inputs.target_environment || 'staging-hostgator' }}
    env:
      TARGET_ENVIRONMENT: ${{ inputs.target_environment || 'staging-hostgator' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Prepare deploy dry-run
        shell: pwsh
        run: ./scripts/prepare-deploy-dry-run.ps1 -TargetEnvironment "$env:TARGET_ENVIRONMENT"

      - name: Publish deploy dry-run summary
        shell: pwsh
        run: Get-Content -Raw artifacts/deploy-dry-run/supersites-deploy-plan.md | Add-Content -LiteralPath $env:GITHUB_STEP_SUMMARY

      - name: Upload deploy dry-run artifact
        uses: actions/upload-artifact@v4.6.2
        continue-on-error: true
        with:
          name: supersites-deploy-dry-run
          path: artifacts/deploy-dry-run
          if-no-files-found: error
